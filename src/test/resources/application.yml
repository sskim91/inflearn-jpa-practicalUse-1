# 메모리 모드로 사용하는 방법
# 그런데 스프링 부트를 사용할 때 세팅이 아무것도 안되어있으면 자동으로 메모리 모드로 테스트하게된다.
spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create  # 자동으로 테이블 만들어줌
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL : debug
    org.hibernate.type : trace